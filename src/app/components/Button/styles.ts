import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        backgroundColor: "#2C43B1",
        height: 48,
        width: "100%",
        borderRadius: 8,
        alignItems: "center",
        justifyContent: "center"
    },
    title: {
        fontWeight: 600,
        color: "#FFF",
        fontSize: 16
    }
})